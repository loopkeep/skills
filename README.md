# loopkeep skills

Agent skills for operating loopkeep from coding agents: investigate runs,
author workflows, tune policies, and use the `lk` CLI.

**Status: pre-release.** Content is mirrored from the loopkeep monorepo.

Once published, install with:

```sh
npx skills add loopkeep/skills
```
