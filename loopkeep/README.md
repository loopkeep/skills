# loopkeep skill

Agent skill for operating loopkeep from coding agents: investigate runs,
author workflows, tune policies, and use the `lk` CLI.

Content is mirrored from the loopkeep monorepo on each release.

Install just this skill:

```sh
npx skills add loopkeep/skills -s loopkeep
```
